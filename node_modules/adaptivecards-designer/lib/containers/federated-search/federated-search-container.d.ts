import * as Adaptive from "adaptivecards";
import { SingleThemeHostContainer } from "../single-theme-host-container";
export declare class FederatedSearchContainer extends SingleThemeHostContainer {
    constructor(name: string, styleSheet: string);
    renderTo(hostElement: HTMLElement): void;
    initialize(): void;
    getHostConfig(): Adaptive.HostConfig;
    get targetVersion(): Adaptive.Version;
    get enableDeviceEmulation(): boolean;
}
