import { HostContainer } from "./host-container";
export declare abstract class SingleThemeHostContainer extends HostContainer {
    private _styleSheet;
    constructor(name: string, styleSheet: string);
    getCurrentStyleSheet(): string;
}
